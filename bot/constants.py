"""
bot/constants.py — единый источник правды для лимитов и общих констант.
Раньше эти значения были рассыпаны (и продублированы) по 3–4 файлам.
"""

# ── Профиль ───────────────────────────────────────────────────────
NAME_MAX_LEN = 16      # единый лимит имени (был 16 при регистрации и 64 при редактировании)
BIO_MAX_LEN  = 500
AGE_MIN      = 14
AGE_MAX      = 99
MAX_PHOTOS   = 5

# ── Свайпы ────────────────────────────────────────────────────────
SWIPE_LIMIT        = 50   # свайпов (лайки + дизы вместе)
SWIPE_WINDOW_HOURS = 6    # скользящее окно в часах

# Повышенный лимит для SHROOM+ (не безлимит — просто сильно больше).
PREMIUM_SWIPE_LIMIT = 500

# Цветной бейдж премиума — ставится префиксом к имени в карточке
# (в титулах SHROOM+ остаётся фирменный ✦).
PREMIUM_BADGE = "🔱"

# ── Платное удаление анкеты (Telegram Stars) ──────────────────────
DELETE_PAYLOAD = "delete_profile_v1"
DELETE_STARS   = 200

# ── SHROOM+ (премиум-подписка, Telegram Stars) ────────────────────
# Тарифы: (дней, звёзд). Источник правды для покупки и админ-выдачи.
PREMIUM_PLANS = [
    (7,   75),
    (30,  250),
    (90,  600),
    (365, 1800),
]
PREMIUM_DAYS_PRESETS = [days for days, _ in PREMIUM_PLANS]  # для админ-выдачи
PREMIUM_PAYLOAD_PREFIX = "premium_v2:"   # payload = premium_v2:<days>
PREMIUM_DEFAULT_DAYS = 30                # дефолт там, где нужен один срок

# ── Рефералы ──────────────────────────────────────────────────────
# Бонус начисляется в «бонусных свайпах» (плюсуются к SWIPE_LIMIT).
REFERRAL_PREFIX         = "ref_"   # payload диплинка: ?start=ref_<id>
REFERRAL_BONUS_SWIPES   = 25       # рефереру за каждого приглашённого
REFERRAL_WELCOME_SWIPES = 25       # новичку, пришедшему по ссылке

# ── Репорты ───────────────────────────────────────────────────────
REPORT_LIMIT_PER_HOUR = 5

# ── Лайк с сообщением ─────────────────────────────────────────────
LIKE_MSG_LIMIT_PER_HOUR = 5     # сколько записок-с-лайком можно отправить в час
LIKE_MSG_MAX_LEN        = 200

REASON_LABELS = {
    "spam":  "📢 спам / реклама",
    "other": "⚙️ другое",
}

# Тексты кнопок нижнего меню (reply-keyboard). Это обычные текстовые
# сообщения — их троттлинг ловит отдельно (см. middlewares/throttle.py),
# т.к. они не команды и не callback, но запускают тяжёлые запросы к БД.
MENU_BUTTON_TEXTS = frozenset({
    "🕯️ лента", "💬 мэтчи", "🩸 лайки", "👁️ профиль",
})
